import * as React from "react";
import * as ReactDOM from "react-dom";

import { Navigator } from "./components/Header";

ReactDOM.render(
    <Navigator height={'80'} bg={'#e4e4e4'} width={'100%'} />,
    document.getElementById("example")
);